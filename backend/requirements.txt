# --- Web Framework -----------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.30.1

# --- Database / ORM -----------------------------------------------------
sqlalchemy==2.0.30
# psycopg2-binary==2.9.9   # disabled: no Python 3.14 wheel yet; re-enable in Milestone-2
alembic==1.13.1

# --- Auth / Security -----------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# --- Validation / Config -----------------------------------------------------
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# --- Data Processing -----------------------------------------------------
pandas==2.2.2
numpy==1.26.4

# --- Future ML (reserved — not used in Milestone-1) -----------------------------------------------------
# scikit-learn==1.5.0
# xgboost==2.0.3
# tensorflow==2.16.1

# --- Testing -----------------------------------------------------
pytest==8.2.0
httpx==0.27.0